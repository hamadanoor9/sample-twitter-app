<h1>All the Users</h1>


<ul>
    <% @users.each do |user| %>
    
    <div class= "user card">
        <%= image_tag url_for(user.avatar), width: '100px' %>
        <br>
        <%= link_to "@#{user.username}", user_url(id: user.id) %>
    </div>
   
    
    <% end %>
</ul>